<?xml version="1.0"?>
<!--
    The browser reads the invalidation map from this block and from nowhere else,
    so this is the one place a narrower map can be handed over.
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:ObjectManager/etc/config.xsd">
    <type name="Magento\Customer\Block\SectionConfig">
        <plugin name="kingletas_section_policy"
                type="Kingletas\SectionPolicy\Plugin\Customer\Block\SectionConfigPlugin"
                sortOrder="10"/>
    </type>
</config>
